# Per-launch configs

Each folder is a launch: edit `constants.ts`, then run the shared CLI:

```bash
bun scripts/v0.7/launch/cli.ts <folder-name> status
```

Copy `_template` to start a new launch. See [`../launch/README.md`](../launch/README.md).
